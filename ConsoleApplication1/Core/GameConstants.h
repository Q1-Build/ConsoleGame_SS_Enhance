#pragma once

namespace ss
{
    // 콘솔 화면과 게임 루프에서 공통으로 사용하는 고정값이다.
    inline constexpr int kScreenWidth = 104;
    inline constexpr int kScreenHeight = 34;
    inline constexpr float kPi = 3.1415926535f;
    inline constexpr unsigned long kTargetFrameMilliseconds = 16;
}
