# SS_Enhance 프로젝트 작업 지침

이 문서는 저장소에서 작업하는 AI 에이전트와 개발자가 가장 먼저 읽는 진입 문서다.
세부 규칙은 `docs` 아래의 주제별 문서에 있으며, 작업 범위에 해당하는 문서를 반드시 함께 읽는다.
구조, 게임 규칙 또는 작업 정책이 바뀌면 코드와 관련 문서를 같은 변경에서 갱신한다.

## 프로젝트 개요

- 프로젝트명: `SS_Enhance`
- 플랫폼: Windows 콘솔
- 언어 및 표준: C++17
- 핵심 경험: `강화 준비 → 실시간 제련 → 강화 판정 → 검의 진화 → 전투와 성장`
- 우선 가치: 명확한 책임, 읽기 쉬운 구조, 재현 가능하고 테스트 가능한 게임 규칙

## 문서 지도

| 문서 | 읽어야 하는 경우 |
| --- | --- |
| [아키텍처](docs/ARCHITECTURE.md) | 파일 추가, 클래스 분리, 계층 또는 의존성 변경 |
| [코딩 컨벤션](docs/CODING_CONVENTIONS.md) | 모든 C++ 코드 작성과 리뷰 |
| [게임 기획과 플레이 방법](docs/GAME_DESIGN.md) | 조작, 밸런스, 장면 흐름, UI 문구 또는 게임 규칙 변경 |
| [개발 절차](docs/DEVELOPMENT_WORKFLOW.md) | 작업 시작, 검증, 완료 판단, 향후 기능 확장 |

## 항상 지켜야 할 핵심 규칙

- `Main.cpp`는 플랫폼 객체 생성과 의존성 조립만 담당하며 게임 로직을 넣지 않는다.
- `Game/Domain`은 입력, 렌더링, Windows API에 의존하지 않는다.
- Windows API는 `Platform/Console`에 격리하고 변경한 콘솔 상태는 RAII로 복구한다.
- 장면은 서로를 직접 생성하지 않고 `SceneTransition`으로 전환을 요청한다.
- 입력, 상태 갱신, 판정, 렌더링을 분리하며 `Render`는 게임 상태를 변경하지 않는다.
- 강화 비용, 확률, 실패 패널티는 `ForgeRules`에서 관리하고 UI가 다시 계산하지 않는다.
- 난수와 시간처럼 테스트를 어렵게 만드는 외부 요소는 경계에서 주입한다.
- 직접적인 `new`, `delete`, 전역 가변 상태, 싱글턴을 사용하지 않는다.
- C++17, Allman 중괄호, 공백 4칸, 프로젝트 명명 규칙을 따른다.
- 새 클래스, public API, 핵심 규칙과 구현 의도에는 규약에 맞는 한글 주석을 작성한다.
- 새 `.h`와 `.cpp`는 Visual Studio 프로젝트 및 필터 파일에 등록한다.
- 코드 변경 후 Debug x64 빌드, 관련 Domain 자동 테스트와 `git diff --check`를 통과시킨다.

## 문서 관리 원칙

- 같은 규칙을 여러 문서에 장황하게 복제하지 않고 가장 적합한 문서 한 곳에서 설명한다.
- `AGENTS.md`에는 모든 작업에 필요한 핵심 경계와 세부 문서 링크만 유지한다.
- 문서의 코드 구조와 현재 구현 설명은 실제 저장소 상태와 일치해야 한다.
- 새 문서가 필요해도 전체 프로젝트 지침 문서는 이 파일을 포함해 5개 이내로 유지한다.
